# References (selective)

This repo selectively references prior projects for patterns/schemas/ideas (no bulk copy).

## opencode-dev (patterns only)

Source: `/mnt/c/Users/chanhokim/Downloads/opencode-dev/opencode-dev`

Used as reference for:
- MCP config shape (`mcp` object with `local` vs `remote`) and documentation patterns.

## csc (legacy workbench)

Source: `/home/chanhokim/projects/new_project/csc`

Used as reference for:
- Workflow SSOT design and phase status semantics (`workflow-daemon`).
- Minimal stdio MCP adapter pattern for workflow transitions (`workflow-mcp`).
- tmux monitor/TUI integration patterns (`tmux/`).

## docker sandbox

Source: `/home/chanhokim/projects/new_project/docker`

Used as reference for:
- Docker sandbox harness concept and resource constraints (`docker/docker/*`).
- MCP tool taxonomy for docker/tmux/workspace/session and safety checks (mount path restrictions) (`mcp-server/src/tools/*`).

